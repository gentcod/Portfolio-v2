import styled from "styled-components";

export const Container = styled.div`
   width: 100%;
   margin-top: 1rem;
   display: flex;
   flex-direction: row;
   column-gap: 2rem;
   backface-visibility: hidden;
`